{
  "name": "3dns-events",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "tsc && node dist/index.js",
    "dns-events": "tsc && node dist/dnsEvents.js",
    "test-dns-packet": "tsc && node dist/testDnsPacketLib.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@types/dns-packet": "^5.6.5",
    "dns-packet": "^5.6.1",
    "ethers": "^6.14.0"
  }
}
